#Validation more detailed (int and float)

from ex113.defs import readint,readfloat

i = readint('Type a integer: ')
f = readfloat('Type a float: ')

print(f'You typed the integer {i} and the float {f}.')